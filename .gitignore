# ── Go ──────────────────────────────────────────────────────────────────────
# Compiled binaries (in service dirs — produced by go build inside each dir)
apps/gateway/gateway
apps/adt-service/adt-service
apps/lab-service/lab-service
apps/fhir-bridge/fhir-bridge
apps/audit-service/audit-service
apps/notification-service/notification-service
apps/simulator/simulator

# Compiled binaries (at repo root — produced by go build ./apps/... from root)
/gateway
/adt-service
/lab-service
/fhir-bridge
/audit-service
/notification-service
/simulator

# Test binary, build artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
bin/

# Vendor directory (not used here, but common)
vendor/

# ── Node / Dashboard ────────────────────────────────────────────────────────
apps/dashboard/node_modules/
apps/dashboard/dist/
apps/dashboard/.vite/

# ── Editor / OS ─────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V500
.Trashes
Thumbs.db

.vscode/
.idea/
*.swp
*.swo
*~

# ── Secrets / credentials ────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.crt

# ── Kubernetes / Helm local overrides ────────────────────────────────────────
kubeconfig
*.kubeconfig
