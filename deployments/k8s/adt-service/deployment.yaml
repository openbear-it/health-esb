---
apiVersion: v1
kind: ConfigMap
metadata:
  name: adt-service-config
  namespace: health-esb
data:
  SERVICE_NAME: "adt-service"
  LOG_LEVEL: "info"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adt-service
  namespace: health-esb
  labels:
    app: adt-service
    app.kubernetes.io/part-of: health-esb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adt-service
  template:
    metadata:
      labels:
        app: adt-service
    spec:
      containers:
      - name: adt-service
        image: ghcr.io/openbear-it/health-esb/adt-service:latest
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: adt-service-config
        env:
        - name: NATS_URL
          value: "nats://nats:4222"
        resources:
          requests:
            memory: "32Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: adt-service
  namespace: health-esb
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: adt-service
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
